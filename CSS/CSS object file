/* 
Which of the following are the true about the CSS property 'Onject-fit'?
Pic one or more options 
*/

(wrong)----->'object-fit: contain;' does not preserve the aspect ratio of the image; it stretches the image to cover the entire width and height of the container.

(correct)----->'object-fit: contain;' preserves the aspect ratio of the image and makes sure no clipping happens to the whole image.

(correct)----->'object-fit: cover;' avoids the image getting squeezed, but it could end up clipping the image.

(wrong)----->'object-fit: cover;' avoids clipping the image by sacrificing the aspect ratio.
